<h2>Edit an Artist HERE</h2>

    <ul>

        <%= form_for @artist do |f| %>
        <%= f.label :name %>
        <%= f.text_field :name, :required => true %><br>

        <%= f.label :age %>
        <%= f.number_field :age, :required => true %> <br>

        <%= f.label :experience %>
        <%= f.select :experience, (1..10) %>

        <%= f.submit "Update Profile" %>
  
        <% end %>

    </ul>